<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MyToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="actionMenuTextColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:actionBarSize">@dimen/toolbar_height</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <item name="actionOverflowButtonStyle">@style/OverflowButtonStyle</item>
    </style>
    <!--设置不覆盖锚点-->
    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
    </style>
    <!-- 右边三个点的样式 -->
    <style name="OverflowButtonStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="android:paddingRight">20dp</item>
        <item name="android:src">?attr/overflowButtonSrc</item>
    </style>

    <!-- Toolbar中标题文字的style -->
    <style name="MyToolbarTitleText">
        <item name="android:textColor">?toolbarTitleTextColor</item>
        <item name="android:textSize">16sp</item>
        <item name="android:singleLine">true</item>
    </style>
    <!-- loading dialog 样式 -->
    <style name="MyDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <attr name="toolbarTitleTextColor" format="color" />
    <attr name="backgroundColor" format="color" />
    <attr name="overflowButtonSrc" format="reference" />

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="toolbarTitleTextColor">@color/white</item>
        <item name="backgroundColor">@color/white</item>

        <item name="overflowButtonSrc">@drawable/abc_ic_menu_overflow_material</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>